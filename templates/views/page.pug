extends ../layouts/default

block content
    if page
        .container(
            data-ks-editable= editable(user, { list: 'Page', id: page.id, path: '/'})
        )
            h3= page.title
            div!= page.content